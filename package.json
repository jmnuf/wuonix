{
  "name": "@jmnuf/wuonix",
  "version": "1.0.1",
  "module": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*.js",
    "dist/*.d.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && rolldown src/*.ts -d dist/ -s inline",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.1.10",
    "rolldown": "^1.0.0-beta.7",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@jmnuf/results": "^1.0.0"
  }
}
